import { Button } from "@repo/ui/components/button";

const HomePage = () => {
    return (
        <div className={"bg-red-200"}>
            <Button className="text-green-600">ClickMe</Button>
        </div>
    );
};

export default HomePage;
